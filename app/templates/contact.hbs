<h1>Contact Page</h1>

<p class="well">If you have any questions please leave a message with your email address.</p>

<div class="row">
  <div class="col-md-6">
    <div class="form-group">
      <label>Email Address*:</label>
      {{input type="email" class="form-control" placeholder="Your Email Address" value=emailAddress autofocus="autofocus"}}

    </div>
    <div class="form-group">
      <label>Your Message*:</label>
      {{textarea class="form-control" placeholder="Your message. (At least 5 characters.)" rows="7" value=message}}
    </div>
    <button class="btn btn-success" {{action 'sendMessages'}} >Send</button>
    </div>

  {{#if responseMessage}}
      <div class="col-md-6">
          <br/>
          <div class="alert alert-success">
              <h4>Thank you! Your message is sent.</h4>
              <p>{{responseMessage}}</p>
          </div>
      </div>
  {{/if}}
</div>
